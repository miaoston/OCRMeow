name: OCRMeow All-in-One Pipeline

on:
  push:
    branches: ["main"]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build-test-deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - uses: actions/checkout@v4

      - name: Use Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20.x"
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Install Fonts (for OCR Test)
        run: |
          sudo apt-get update
          sudo apt-get install -y fonts-noto-cjk fonts-wqy-zenhei

      - name: Install Puppeteer Browsers
        run: npx puppeteer browsers install chrome

      - name: Run code checks (lint, format, type-check)
        run: npm run check

      - name: Build (Lean - No Models)
        run: npm run build

      - name: Run OCR Easter Egg Test
        run: node tests/run_ocr_test.js

      - name: Package Lean ZIP
        run: |
          cd dist
          zip -r ../ocrmeow-lean.zip .
          cd ..

      - name: Download Models for Bundled Build
        run: |
          mkdir -p public/models
          curl -L "https://paddle-model-ecology.bj.bcebos.com/paddlex/official_inference_model/paddle3.0.0/PP-OCRv5_mobile_det_onnx.tar" -o public/models/det.tar
          curl -L "https://paddle-model-ecology.bj.bcebos.com/paddlex/official_inference_model/paddle3.0.0/PP-OCRv5_mobile_rec_onnx.tar" -o public/models/rec.tar

      - name: Build (Bundled - With Models)
        run: npm run build

      - name: Package Bundled ZIP
        run: |
          cd dist
          zip -r ../ocrmeow-bundled.zip .
          cd ..

      - name: Upload Lean ZIP
        uses: actions/upload-artifact@v4
        with:
          name: ocrmeow-lean
          path: ocrmeow-lean.zip
          retention-days: 14

      - name: Upload Bundled ZIP
        uses: actions/upload-artifact@v4
        with:
          name: ocrmeow-bundled
          path: ocrmeow-bundled.zip
          retention-days: 14

      - name: Setup Pages
        uses: actions/configure-pages@v4

      - name: Upload Pages Artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: "./dist"

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
